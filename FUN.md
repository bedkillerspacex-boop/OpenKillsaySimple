# OpenKillsaySimple Fun Zone

这里放一点不影响证据链的整活。

## 小猫弟行为模拟器

```text
if (evidence.isHard()) {
    say("这是巧合");
} else if (refmap.name().equals("KillsayReborn-refmap.json")) {
    say("构建工具的问题");
} else if (className.equals("PendingVictim")) {
    say("大家都会这么写");
} else {
    say("你们不懂 Simple");
}
```

## Simple 牌压缩机

```text
输入:
  KillsayReborn LTSC 2.0
  HUD
  自动回复
  自动举报
  云文本
  歌词
  发送管线
  WebGuiServer
  MinecraftWorldProbe

输出:
  KillsaySimple
  README 嘴硬
  一堆乱码
```

## 免责声明

整活归整活，证据归证据。  
真正用于对线时，请优先引用 [paste/README.md](paste/README.md) 里的可核验证据，不要拿段子当证据。

