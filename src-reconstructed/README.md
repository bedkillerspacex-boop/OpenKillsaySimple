# Reconstructed Source Notes

These files are not original sources and are not intended to compile.

They are readable reconstructions of the `KillsaySimple-1.0.0.jar` structure based on:

- JAR file tree
- `fabric.mod.json`
- `killsay.mixins.json`
- `KillsayReborn-refmap.json`
- `javap -p -c -constants` output

Use them as an index for reading the evidence report, not as a replacement for bytecode proof.

